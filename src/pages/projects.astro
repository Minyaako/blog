---
import BaseLayout from '../layouts/BaseLayout.astro'
---
<BaseLayout title="项目" description="Minyako 的项目与长期制作记录。"><div class="projects"><header><p>Projects</p><h1>项目</h1></header><section><span>01 / Preparing</span><h2>第一份公开项目记录正在整理</h2><p>这里将展示有持续维护价值的研究、工具与创作项目。首版先保留稳定入口，不用虚构内容填满页面。</p><a href="/domains/engineering">先阅读技术构建记录 →</a></section></div></BaseLayout>
<style>.projects{width:min(100% - 2rem,var(--content-wide));min-height:65vh;margin-inline:auto;padding-block:var(--space-7)}header p,section span{color:var(--text-muted);font:var(--text-caption)/1.4 var(--font-mono);letter-spacing:.1em;text-transform:uppercase}h1{margin:var(--space-2) 0 var(--space-7);font:760 clamp(2.5rem,8vw,5rem)/1 var(--font-display)}section{max-width:48rem;padding:clamp(1.5rem,5vw,3rem);border:1px solid var(--border);border-radius:var(--radius-lg);background:var(--surface)}h2{font:700 var(--text-title)/1.3 var(--font-display)}section p{color:var(--text-muted);line-height:1.7}</style>
