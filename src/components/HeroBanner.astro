<figure class="hero-banner">
  <img src="/images/home/banner-geometric.svg" alt="Minyako 首页横幅" width="1600" height="520" />
  <figcaption>Current scenery · Original geometric study by Minyako</figcaption>
</figure>

<style>
  .hero-banner {
    position: relative;
    overflow: hidden;
    aspect-ratio: 40 / 13;
    margin: 0;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
    box-shadow: var(--shadow-soft);
  }

  img { width: 100%; height: 100%; object-fit: cover; }

  figcaption {
    position: absolute;
    inset: auto auto var(--space-3) var(--space-4);
    color: #f7f2eb;
    font-family: var(--font-mono);
    font-size: 0.67rem;
    letter-spacing: 0.08em;
    text-shadow: 0 1px 6px rgb(0 0 0 / 0.65);
  }

  @media (max-width: 40rem) {
    .hero-banner { aspect-ratio: 16 / 8.5; }
    figcaption { inset-inline: var(--space-3); }
  }
</style>
